<% if @error.blank? %>
$('.loading').addClass('is-hide');
$('#create-video').html('<%= j(render 'shared/progress_bar', content_id: @article_uid, user_id: @user_uid) %>');
// 閉じるアイコンを消す＆モーダルを閉じられなくする。
//var $closeModal = $('#display-modal-1').children('.close-modal');
//$closeModal.html('');
//$closeModal.off();

<% else %>

var articlePath = "<%= article_path(@article)%>";
window.location.href = articlePath;

<% end %>