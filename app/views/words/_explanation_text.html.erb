<% color ||= nil %>
<% text&.each_line do |line| %>

  <!-- inline要素の謎の隙間を埋めるために、親要素の文字間を縮めて、子要素でletter-spacing: normalを指定する 参考：https://qiita.com/BEMU/items/0f500e0306471073ed63 -->
  <div class="line-with-dict <%= color %> d-block my-1" style="letter-spacing: 0.1em;">
    <%= render 'shared/line_with_dict_partly',  line: line, lang_number: lang_number %>
  </div>

<% end %>